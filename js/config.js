// Public runtime configuration.
// Set googleMapsApiKey to a browser key restricted by HTTP referrer.
// Enable Maps JavaScript API and Places API (New) on the same Google Cloud project.
window.ROUTETOOLS_CONFIG = {
  googleMapsApiKey: '',
};
